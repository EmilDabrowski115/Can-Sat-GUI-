char *fileToString(const char *filename)
{
    FILE *f = fopen(filename, "r");
    if (f == NULL) {
        printf("errno: %d, couldn't load file: %s\n", errno, filename);
        exit(-1);
    }
    fseek(f, 0, SEEK_END);
    long fsize = ftell(f);
    fseek(f, 0, SEEK_SET);

    char *string = malloc(fsize + 1);
    fread(string, 1, fsize, f);
    fclose(f);

    string[fsize] = 0;
    
        string readText = File.ReadAllText(shaders.fv);      
        string readText = File.ReadAllText(shaders.vs); 

        Console.WriteLine(readText);

    return string;
}

 